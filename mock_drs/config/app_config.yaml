# General server settings
server:
    host: 0.0.0.0
    port: 9101
    debug: True

# DRS service info settings
service_info:
    contact_info: 'https://github.com/elixir-europe/mock-DRS'
    supported_file_system_protocols:
        - http
    version: ''

# Location of OpenAPI specs
openapi:
    path: 'specs/schema.data_repository_service.cd0186f.openapi.modified.yaml'

# Database settings
database:
    host: 'mongo'
    port: 27017
    name: drs-db
    object_id:
        length: 4
        charset: string.ascii_uppercase + string.digits
